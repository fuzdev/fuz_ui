---
'@fuzdev/fuz_ui': minor
---

add `MdzRoot` context provider; switch `mdz_components_context` and `mdz_elements_context` to the getter pattern with ancestor fallback (`set_mdz_context_with_fallback`)

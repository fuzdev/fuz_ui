---
'@fuzdev/fuz_ui': patch
---

feat: accept svg data and strings for the `ContextmenuEntry` and `ContextmenuSubmenu` `icon`, rendered via the new shared `ContextmenuIcon`, and make the `ContextmenuTextEntry` `icon` optional

/**
 * This is a freestanding comment without `@module` tag.
 *
 * Under the old heuristic this would be extracted.
 * Under the new `@module` requirement it should not be.
 */

export const value = 42;
